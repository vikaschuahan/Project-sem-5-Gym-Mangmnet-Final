apiVersion: v1
kind: Service
metadata:
  name: gym-web-service
  namespace: gym-management
spec:
  selector:
    app: gym-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP          # Use LoadBalancer if not using Ingress
